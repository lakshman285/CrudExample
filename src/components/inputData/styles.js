import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  textInputView: {
    marginLeft: 35,
    marginRight: 35,
    marginTop: 10,
    borderColor: 'gray',
    borderWidth: 1,
  },
});

export default Styles;
